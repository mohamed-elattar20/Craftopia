export type RegisterData = {
    firstName: string,
    lastName: string,
    email: string,
    brand: string,
    phone: string,
    address: string
    city: string
    confirmPassword: string
    governorate: { value: string, label: string }
    password: string
}